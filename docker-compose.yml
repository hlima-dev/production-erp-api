services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: erp_producao
      POSTGRES_USER: erp
      POSTGRES_PASSWORD: erp
    ports:
      - "5432:5432"
    volumes:
      - erp_pg_data:/var/lib/postgresql/data

volumes:
  erp_pg_data:
